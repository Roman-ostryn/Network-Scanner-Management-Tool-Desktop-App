<?xml version="1.0" encoding="utf-8"?>
<Window x:Class="SC.AgentDeployer.Client.Views.ClientInfoWindow" xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" xmlns:d="http://schemas.microsoft.com/expression/blend/2008" xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" xmlns:i="clr-namespace:System.Windows.Interactivity;assembly=System.Windows.Interactivity" xmlns:viewModels="clr-namespace:SC.AgentDeployer.Client.ViewModels" xmlns:helpers="clr-namespace:SC.AgentDeployer.Client.Helpers" xmlns:converters="clr-namespace:SC.AgentDeployer.Client.ValueConverters" x:Name="window" Title="Client Details" Height="450" MinHeight="450" Width="390" MinWidth="390" WindowStartupLocation="CenterScreen" xmlns:views="clr-namespace:SC.AgentDeployer.Client.Views">
  <FrameworkElement.DataContext>
    <viewModels:ClientInfoViewModel />
  </FrameworkElement.DataContext>
  <UIElement.InputBindings>
    <KeyBinding Key="Escape" Command="{Binding CloseCommand}" />
  </UIElement.InputBindings>
  <i:Interaction.Triggers>
    <i:EventTrigger i:EventTrigger.EventName="CloseRequested" i:EventTriggerBase.SourceObject="{Binding Mode=OneWay}">
      <helpers:CallMethodAction MethodName="Close" TargetObject="{Binding ElementName=window}" />
    </i:EventTrigger>
  </i:Interaction.Triggers>
  <Grid Margin="5">
    <Border Grid.Row="0" BorderBrush="#FF000000" BorderThickness="1">
      <ScrollViewer VerticalScrollBarVisibility="Auto">
        <Grid Margin="5">
          <Grid.ColumnDefinitions>
            <ColumnDefinition Width="350" />
          </Grid.ColumnDefinitions>
          <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
          </Grid.RowDefinitions>
          <StackPanel Grid.Row="0" Orientation="Vertical">
            <TextBlock Style="{StaticResource App.TextBlock.Title}" Text="Client Service Name:" />
            <TextBox Style="{StaticResource App.TextBox.Readonly.Value}" Text="{Binding ClientServiceName, Mode=OneWay}" />
          </StackPanel>
          <StackPanel Grid.Row="1" Orientation="Vertical">
            <TextBlock Style="{StaticResource App.TextBlock.Title}" Text="Client Version:" />
            <TextBox Style="{StaticResource App.TextBox.Readonly.Value}" Text="{Binding ClientServiceVersion, Mode=OneWay}" />
          </StackPanel>
          <StackPanel Grid.Row="2" Orientation="Vertical">
            <TextBlock Style="{StaticResource App.TextBlock.Title}" Text="Relay Uri:" />
            <TextBox Style="{StaticResource App.TextBox.Readonly.Value}" Text="{Binding ClientServiceRelayUrl, Mode=OneWay}" />
          </StackPanel>
          <StackPanel Grid.Row="3" Orientation="Vertical">
            <TextBlock Style="{StaticResource App.TextBlock.Title}" Text="{Binding Source={StaticResource LabelResources.CustomProperty1.Text}}" />
            <TextBox Style="{StaticResource App.TextBox.Editable.Value}" Text="{Binding CustomProperty1, Mode=TwoWay}" />
          </StackPanel>
          <StackPanel Grid.Row="4" Orientation="Vertical">
            <TextBlock Style="{StaticResource App.TextBlock.Title}" Text="{Binding Source={StaticResource LabelResources.CustomProperty2.Text}}" />
            <TextBox Style="{StaticResource App.TextBox.Editable.Value}" Text="{Binding CustomProperty2, Mode=TwoWay}" />
          </StackPanel>
          <StackPanel Grid.Row="5" Orientation="Vertical">
            <TextBlock Style="{StaticResource App.TextBlock.Title}" Text="{Binding Source={StaticResource LabelResources.CustomProperty3.Text}}" />
            <TextBox Style="{StaticResource App.TextBox.Editable.Value}" Text="{Binding CustomProperty3, Mode=TwoWay}" />
          </StackPanel>
          <StackPanel Grid.Row="6" Orientation="Vertical">
            <TextBlock Style="{StaticResource App.TextBlock.Title}" Text="{Binding Source={StaticResource LabelResources.CustomProperty4.Text}}" />
            <TextBox Style="{StaticResource App.TextBox.Editable.Value}" Text="{Binding CustomProperty4, Mode=TwoWay}" />
          </StackPanel>
          <StackPanel Grid.Row="7" Orientation="Vertical">
            <Grid Margin="0,10,0,0">
              <Grid.ColumnDefinitions>
                <ColumnDefinition Width="190" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="80" />
                <ColumnDefinition Width="80" />
              </Grid.ColumnDefinitions>
              <Button IsDefault="True" Focusable="True" Grid.Column="2" Content="Save" Width="75" HorizontalAlignment="Right" Style="{DynamicResource App.ButtonStyle}" Margin="0,0,5,0" Command="{Binding SaveCommand}" />
              <Button IsCancel="True" Focusable="True" Grid.Column="3" Content="Cancel" Width="75" HorizontalAlignment="Right" Style="{DynamicResource App.ButtonStyle}" Margin="0,0,5,0" Command="{Binding CloseCommand}" />
            </Grid>
          </StackPanel>
        </Grid>
      </ScrollViewer>
    </Border>
  </Grid>
</Window>