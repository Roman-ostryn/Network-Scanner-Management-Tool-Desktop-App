<?xml version="1.0" encoding="utf-8"?>
<Window x:Class="SC.AgentDeployer.Client.Views.DomainCredentialsWindow" xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" xmlns:d="http://schemas.microsoft.com/expression/blend/2008" xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" xmlns:i="clr-namespace:System.Windows.Interactivity;assembly=System.Windows.Interactivity" xmlns:viewModels="clr-namespace:SC.AgentDeployer.Client.ViewModels" xmlns:helpers="clr-namespace:SC.AgentDeployer.Client.Helpers" x:Name="window" Title="Change User" Height="200" MinHeight="200" Width="335" MinWidth="335" WindowStartupLocation="CenterScreen" xmlns:views="clr-namespace:SC.AgentDeployer.Client.Views">
  <FrameworkElement.DataContext>
    <viewModels:DomainCredentialsViewModel />
  </FrameworkElement.DataContext>
  <UIElement.InputBindings>
    <KeyBinding Modifiers="Control" Key="S" Command="{Binding SaveCredentialsCommand}" />
    <KeyBinding Key="Escape" Command="{Binding CloseCommand}" />
  </UIElement.InputBindings>
  <i:Interaction.Triggers>
    <i:EventTrigger i:EventTrigger.EventName="CloseRequested" i:EventTriggerBase.SourceObject="{Binding Mode=OneWay}">
      <helpers:CallMethodAction MethodName="Close" TargetObject="{Binding ElementName=window}" />
    </i:EventTrigger>
  </i:Interaction.Triggers>
  <Grid>
    <StackPanel Orientation="Vertical" Grid.Row="0" Margin="10">
      <StackPanel Orientation="Horizontal">
        <TextBlock Margin="5,0,0,0">Perform Operation As:</TextBlock>
        <TextBlock Margin="5,0,0,10" Text="{Binding CurrentUser, Mode=OneWay, UpdateSourceTrigger=PropertyChanged}" />
      </StackPanel>
      <Grid Margin="0">
        <Grid.ColumnDefinitions>
          <ColumnDefinition Width="110" />
          <ColumnDefinition Width="180" />
        </Grid.ColumnDefinitions>
        <Label Grid.Column="0">Domain Name:</Label>
        <TextBox Grid.Column="1" VerticalAlignment="Center" Width="180" Text="{Binding DomainName, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
      </Grid>
      <Grid Margin="0">
        <Grid.ColumnDefinitions>
          <ColumnDefinition Width="110" />
          <ColumnDefinition Width="180" />
        </Grid.ColumnDefinitions>
        <Label Grid.Column="0">Domain User:</Label>
        <TextBox Grid.Column="1" VerticalAlignment="Center" Width="180" Text="{Binding DomainUser, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
      </Grid>
      <Grid Margin="0">
        <Grid.ColumnDefinitions>
          <ColumnDefinition Width="110" />
          <ColumnDefinition Width="180" />
        </Grid.ColumnDefinitions>
        <Label Grid.Column="0">Domain Password:</Label>
        <PasswordBox Name="passwordBox" Grid.Column="1" VerticalAlignment="Center" Width="180" />
      </Grid>
      <Grid Margin="0,10,0,0">
        <Grid.ColumnDefinitions>
          <ColumnDefinition Width="135" />
          <ColumnDefinition Width="Auto" />
          <ColumnDefinition Width="80" />
          <ColumnDefinition Width="80" />
        </Grid.ColumnDefinitions>
        <TextBlock Grid.Column="0" HorizontalAlignment="Left" VerticalAlignment="Center" Margin="5,0,0,0" Foreground="{StaticResource App.Text.Error}" Text="{Binding StatusMessage}" />
        <Button IsCancel="True" Grid.Column="3" Content="Cancel" Width="75" HorizontalAlignment="Right" Style="{DynamicResource App.ButtonStyle}" Margin="0,0,5,0" Command="{Binding CloseCommand}" />
        <Button IsDefault="True" Focusable="True" Grid.Column="2" Content="Save" Width="75" HorizontalAlignment="Right" Style="{DynamicResource App.ButtonStyle}" Margin="0,0,5,0" Command="{Binding SaveCredentialsCommand}" CommandParameter="{Binding ElementName=passwordBox}" />
      </Grid>
    </StackPanel>
  </Grid>
</Window>